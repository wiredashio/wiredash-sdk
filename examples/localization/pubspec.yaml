name: wiredash_localization
description: Sample project demonstrating localization
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  intl: any
  wiredash:
    path: ../../

dev_dependencies:
  lint: ^1.0.0

flutter:
  uses-material-design: true
